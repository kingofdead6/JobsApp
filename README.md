# بحث عن عمل DZ — منصّة التشغيل الجزائرية

تنفيذ كامل لدفتر الشروط: تطبيق محمول بـ Flutter، واجهة خلفية REST بـ Express،
لوحة إدارة وموقع تحميل بـ React + Tailwind.

```
JobsPlatdorm/
├── backend/   ← Express + MongoDB (REST API + Socket.IO)
├── mobile/    ← تطبيق Flutter (عربي، RTL)
├── admin/     ← لوحة الإدارة (React + Vite + Tailwind)
└── website/   ← صفحة تحميل التطبيق (React + Vite + Tailwind)
```

---

## المتطلّبات

| الأداة | الإصدار |
|---|---|
| Node.js | 18 فما فوق (مُختبَر على 22) |
| MongoDB | 6 فما فوق، يعمل على المنفذ 27017 |
| Flutter | 3.19 فما فوق (للتطبيق المحمول) |

---

## 1. الواجهة الخلفية (backend)

```bash
cd backend
npm install
cp .env.example .env     # عدّل JWT_SECRET ومفاتيح Cloudinary
npm run seed             # بيانات أوّلية للتجريب
npm run dev              # http://localhost:5000
```

### تخزين الملفات — Cloudinary

السير الذاتية (PDF) والشعارات والصور الشخصية تُرفع إلى **Cloudinary**.
اضبط المفاتيح الثلاثة في `.env`:

```
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
```

تجدها في [لوحة تحكّم Cloudinary](https://console.cloudinary.com/settings/api-keys).

- **إن تُركت فارغة**: تُخزَّن الملفات محليًا في `uploads/` — للتطوير فقط.
- **ضرورية على Render**: نظام ملفات Render مؤقّت، وكل إعادة نشر تمحو
  ما رُفع محليًا، فبدون Cloudinary تضيع ملفات المستخدمين.

الصور تُضغط تلقائيًا (حد أقصى 1200×1200، جودة تلقائية)، وملفات PDF تُرفع
بنوع `raw`. عند استبدال ملف يُحذف القديم من Cloudinary تلقائيًا.

### حسابات التجريب

| الدور | الهاتف | كلمة المرور |
|---|---|---|
| مشرف | `0550000000` | `admin123456` |
| باحث عن عمل | `0555121456` | `seeker123456` |
| مؤسسة | `0550120001` | `company123456` |

> باقي المؤسسات: `0550120002` … `0550120006` بنفس كلمة المرور.

### الاختبارات

```bash
npm test          # 72 اختبارًا لكل وحدات الإصدار الأول
npm run test:admin  # 20 اختبارًا لمسارات لوحة الإدارة
```

الخادم يجب أن يكون قيد التشغيل. **شغّل `npm run seed` قبل كل تشغيل للاختبارات**،
فهي تعتمد على حالة ابتدائية معروفة وتُنشئ بيانات أثناء عملها.

### رمز التأكيد (OTP)

بدون مزوّد رسائل قصيرة، يُطبع الرمز في سجلّ الخادم ويُعاد في الاستجابة
تحت `devOtp` لتسهيل الاختبار. عند الإنتاج: اضبط `SMS_PROVIDER_KEY` وأكمل
الإدماج في [`smsService.js`](backend/src/services/smsService.js).

---

## 2. لوحة الإدارة (admin)

```bash
cd admin
npm install
npm run dev              # http://localhost:5173
```

تُمرَّر نداءات `/api` إلى الخادم على المنفذ 5000 تلقائيًا (إعداد proxy في Vite).
الدخول محصور بحسابات `role: admin`.

الصفحات: لوحة القيادة · مصادقة العروض · المؤسسات والتوثيق · المستخدمون ·
البلاغات · المحتوى الترويجي والإشعارات الجماعية.

---

## 3. التطبيق المحمول (mobile)

```bash
cd mobile
flutter pub get
flutter run
```

### بناء نسخة الإصدار

```bash
flutter build apk --release
# الناتج: build/app/outputs/flutter-apk/app-release.apk  (~57 ميغابايت)
```

### ⚠️ حزمة Flutter على هذا الجهاز

النسخة المثبّتة في `C:\Program Files\flutter` **غير صالحة للاستعمال**: المسار
لا يملك فيه المستخدم صلاحية الكتابة، فيفشل إنشاء `bin\cache\lockfile`
وتتوقّف كل أوامر Flutter عن الاستجابة دون رسالة خطأ.

تُستعمل بدلًا منها النسخة الموجودة في **`C:\flutter`** (نسخة كاملة يملك
المستخدم صلاحياتها). إمّا تُشغَّل الأوامر بمسارها الكامل:

```powershell
C:\flutter\bin\flutter.bat build apk --release
```

أو يُحدَّث متغيّر PATH ليشير إلى `C:\flutter\bin` بدل `C:\Program Files\flutter\bin`.

### تعديلات Gradle اللازمة

- **`compileSdk = 36`** في `android/app/build.gradle.kts`.
- **فرض `compileSdk` موحّدًا على كل المشاريع الفرعية** في
  `android/build.gradle.kts`: إضافتا `file_picker` و`share_plus` تُصرَّفان
  مقابل API 34 بينما تتطلّب تبعياتهما 36، فيفشل البناء دون هذا الفرض.
  يُسجَّل عبر `afterEvaluate` **قبل** `evaluationDependsOn(":app")`، وإلّا
  فات الأوان لأن المشاريع تكون قد قُيّمت.

### التوقيع

نسخة الإصدار موقّعة حاليًا **بمفاتيح التطوير (debug)** — كافية للتجريب
والتثبيت المباشر، لكن Google Play يرفضها. قبل النشر: أنشئ keystore
وأضف `signingConfig` حقيقيًا في `android/app/build.gradle.kts`.

كذلك معرّف التطبيق هو `com.example.bahth_aan_amal_dz`، و**Google Play يرفض
أي معرّف يبدأ بـ `com.example`** — يجب تغييره قبل النشر.

### عنوان الخادم

التطبيق يتّصل افتراضيًا بالخادم المنشور:

```
https://jobsapp-5pvi.onrender.com
```

للتطوير على خادم محلّي مرّر العنوان عند التشغيل:

```bash
# محاكي Android (10.0.2.2 = جهاز المضيف من داخل المحاكي)
flutter run --dart-define=API_URL=http://10.0.2.2:5000

# هاتف حقيقي على نفس الشبكة
flutter run --dart-define=API_URL=http://192.168.1.X:5000
```

> الخطّة المجانية على Render تُنيم الخادم بعد فترة خمول، فقد يستغرق
> أوّل نداء بعد النوم ~50 ثانية. الشاشات تعرض حالة تحميل في هذه الأثناء.

### الخط العربي

التصميم يستعمل خط **Cairo**. حمّله من
[Google Fonts](https://fonts.google.com/specimen/Cairo)، ضع الملفات في
`mobile/assets/fonts/`، ثم أزل التعليق عن كتلة `fonts:` في
[`pubspec.yaml`](mobile/pubspec.yaml). بدونه يعمل التطبيق بالخط الافتراضي.

---

## 4. موقع التحميل (website)

```bash
cd website
npm install
npm run dev              # http://localhost:5174
npm run build            # الناتج في dist/
```

صفحة هبوط عربية (RTL) يحمّل منها المستخدمون ملف APK.

> ⚠️ **رابط التحميل فارغ حاليًا.** ارفع
> `mobile/build/app/outputs/flutter-apk/app-release.apk` إلى GitHub Releases
> أو Google Drive، ثم ضع الرابط في
> [`website/src/config.js`](website/src/config.js). ما دام فارغًا يعرض
> الموقع زرًّا معطّلًا «سيتوفّر التحميل قريبًا» بدل رابط مكسور.

التفاصيل الكاملة في [`website/README.md`](website/README.md).

---

## تغطية دفتر الشروط

| الفصل | الوحدة | الحالة |
|---|---|---|
| 3.1 | الحساب والمصادقة (OTP، استرجاع، حذف) | ✅ |
| 3.2 | الواجهة الرئيسية (بحث سريع، 6 مداخل، لافتة) | ✅ |
| 3.3 | البحث والتصفية (58 ولاية، عقود، رواتب، بحث محفوظ) | ✅ |
| 3.4 | تفاصيل العرض والترشّح (حفظ، مشاركة، إبلاغ) | ✅ |
| 3.5 | السيرة الذاتية الرقمية (مؤشّر اكتمال، رفع PDF) | ✅ |
| 3.6 | نشر العرض بمعالج 3 خطوات + تسيير العروض | ✅ |
| 3.7 | المراسلة والإشعارات (Socket.IO آني) | ✅ |
| 3.8 | الشركات والتوثيق والعروض المميّزة | ✅ |
| 3.9 | التوصية الذكية | ✅ |
| 3.9 | وضع العمل دون اتصال | ⬜ لاحقًا |
| 3.9 | واجهة بالفرنسية والأمازيغية | ⬜ البنية جاهزة (`locale`) |
| 3.9 | التقييم المتبادل | ⬜ الحقول موجودة في النموذج |
| 4 | لوحة الإدارة كاملة | ✅ |
| 5 | الأمن، RTL، القانون 18-07 | ✅ |

### المتطلّبات غير الوظيفية (الفصل 5)

- **الأمن**: bcrypt (12 جولة)، JWT، صلاحيات حسب الدور، `rate limiting`،
  Helmet، تحقّق صارم من المدخلات، رفع ملفات بأسماء عشوائية وأنواع مقيّدة.
- **حماية المعطيات**: موافقة صريحة عند التسجيل، حقّ الاطّلاع والحذف النهائي،
  لا تُعاد كلمات المرور ولا رموز OTP إلى العميل أبدًا.
- **RTL**: مفروض على مستوى `MaterialApp` في كل الشاشات.
- **إمكانية الوصول**: أهداف لمس ≥ 44 بكسل، مقياس خط محدود بين 0.85 و1.4.
- **الأداء**: نداء `/api/home` واحد للواجهة الرئيسية، فهارس مركّبة على
  حقول التصفية، وفهرس نصّي للبحث العربي.

---

## ملاحظات معمارية

- **مصادقة العروض إجبارية**: كل عرض يُنشأ بحالة `pending`، ولا يظهر في أي
  نتيجة عامة قبل موافقة المشرف — وأي تعديل يُعيده إلى المراجعة.
- **المهام الدورية** (انتهاء صلاحية العروض، تنبيهات البحث المحفوظ) تعمل
  داخل العملية عبر `setInterval`. عند التوسّع إلى أكثر من نسخة من الخادم،
  انقلها إلى مجدول خارجي لتفادي التكرار.
- **الإشعارات** تُحفظ في القاعدة وتُبثّ آنيًا عبر Socket.IO. إدماج
  Firebase (FCM) موضعه محدّد في
  [`notificationService.js`](backend/src/services/notificationService.js).
- **الدفع** (SATIM / الذهبية) غير مُدمج؛ الاشتراكات والإبراز يُفعّلها
  المشرف يدويًا من اللوحة حاليًا.
